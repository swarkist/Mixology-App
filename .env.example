# Server Configuration
NODE_ENV=development
PORT=5000
# Add your mobile device IP or other local origins here
CORS_ORIGINS=http://192.168.50.2:5000,http://localhost:5000,http://192.168.50.2:8081

# Security Secrets (Required)
JWT_SECRET=dev_jwt_secret_change_me
REFRESH_SECRET=dev_refresh_secret_change_me
ADMIN_API_KEY=dev_admin_key

# Firebase Emulator Configuration (Option B)
# If these are set, FIREBASE_SERVICE_ACCOUNT_JSON is NOT required.
GCLOUD_PROJECT=miximixology-dev
FIREBASE_PROJECT_ID=miximixology-dev
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

# Firebase Admin SDK (Only required for Cloud Mode / Production)
# Paste the minified JSON content of your service account key here
# FIREBASE_SERVICE_ACCOUNT_JSON=

# Optional / Feature Specific
# OPENROUTER_API_KEY=
# ADMIN_EMAIL=
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# FROM_EMAIL=
